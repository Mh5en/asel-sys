# دليل الرفع التدريجي للملفات على GitHub (30 يوم)

هذا الدليل يوضح كيفية رفع الملفات تدريجياً على GitHub لمدة 30 يوم، بحيث يبدو أنك بدأت المشروع للتو وتعمل عليه تدريجياً.

---

## 📋 كيف يعمل النظام:

### الخطوة 1: تقسيم الملفات
- تم تقسيم **147 ملف** على **30 يوم**
- كل يوم سيتم رفع **5 ملفات تقريباً**
- الملفات موزعة بشكل منطقي (ملفات HTML، ثم CSS، ثم JS، إلخ)

### الخطوة 2: الرفع اليومي
- كل يوم في الساعة 9:00 صباحاً، يتم تشغيل السكريبت تلقائياً
- السكريبت يضيف الملفات المخصصة لهذا اليوم
- يعمل commit برسالة: "Day X: Add project files (Y files)"
- يرفع التغييرات إلى GitHub

---

## 🚀 خطوات الإعداد:

### 1. تنظيف المستودع على GitHub

**مهم جداً:** يجب أن تبدأ بمستودع فارغ أو تحذف جميع الملفات من GitHub:

1. اذهب إلى: https://github.com/Mh5en/asel-sys
2. احذف جميع الملفات من المستودع (أو أنشئ مستودع جديد فارغ)
3. اترك فقط ملف README.md بسيط (اختياري)

### 2. إعداد المشروع المحلي

```powershell
# تأكد من أنك في مجلد المشروع
cd C:\Users\Mohamed\Desktop\asel-sys

# قم بتشغيل سكريبت الإعداد (تم تشغيله بالفعل)
.\setup-gradual-push.ps1
```

### 3. تحديث المهمة المجدولة

```powershell
# قم بتشغيل سكريبت الإعداد للمهمة المجدولة
.\setup-daily-push-simple.ps1
```

---

## 📊 توزيع الملفات:

### اليوم 1:
- `.FullName`
- `.gitignore`
- `DATABASE_ERD.md`
- `DATABASE_SCHEMA_AR.md`
- `GITHUB-DAILY-PUSH-SETUP.md`

### اليوم 2:
- `README-DAILY-PUSH.md`
- `README.md`
- `SYSTEM_DESIGN.md`
- `SYSTEM_DOCUMENTATION_AR.md`
- `TESTING_STRATEGY_AR.md`

### وهكذا حتى اليوم 30...

---

## 🧪 اختبار النظام:

### اختبار يدوي:

```powershell
# شغّل السكريبت يدوياً للاختبار
.\daily-gradual-push.ps1
```

### التحقق من التقدم:

```powershell
# اقرأ ملف التقدم
cat .daily-push-progress.txt
```

---

## 📝 ملاحظات مهمة:

1. **ابدأ بمستودع فارغ:** يجب أن يكون المستودع على GitHub فارغاً أو يحتوي على ملفات قليلة فقط

2. **لا تعدل ملفات التوزيع:** لا تعدل ملف `.file-distribution-plan.txt` أو `.daily-push-progress.txt`

3. **الملفات المحلية:** جميع الملفات موجودة محلياً، لكن سيتم رفعها تدريجياً على GitHub

4. **التزامن:** بعد كل رفع يومي، يمكنك متابعة التقدم على GitHub

---

## 🔍 متابعة التقدم:

### على GitHub:
- اذهب إلى: https://github.com/Mh5en/asel-sys/commits/main
- ستجد commit جديد كل يوم
- كل commit يحتوي على 5 ملفات تقريباً

### محلياً:
- ملف `.daily-push-progress.txt` يحتوي على التقدم الحالي
- ملف `.file-distribution-plan.txt` يحتوي على خطة التوزيع

---

## ⚙️ إدارة المهمة المجدولة:

### عرض المهمة:
```powershell
Get-ScheduledTask -TaskName "DailyGitHubPush-AselSys"
```

### إيقاف المهمة (مؤقت):
```powershell
Disable-ScheduledTask -TaskName "DailyGitHubPush-AselSys"
```

### تفعيل المهمة:
```powershell
Enable-ScheduledTask -TaskName "DailyGitHubPush-AselSys"
```

### تغيير وقت التشغيل:
1. افتح **Task Scheduler**
2. ابحث عن: `DailyGitHubPush-AselSys`
3. انقر بزر الماوس الأيمن → **Properties**
4. اذهب إلى تبويب **Triggers**
5. عدّل الوقت المطلوب

---

## 🎯 النتيجة المتوقعة:

بعد 30 يوم:
- ✅ 30 commit على GitHub
- ✅ جميع الملفات (147 ملف) موزعة على 30 يوم
- ✅ يبدو أنك بدأت المشروع للتو وتعمل عليه تدريجياً
- ✅ سجل كامل للتقدم على GitHub

---

## 💡 نصائح:

1. **ابدأ بمستودع فارغ** - هذا مهم جداً
2. **اختبر السكريبت يدوياً** قبل الاعتماد عليه
3. **راقب الرفع** في الأيام الأولى
4. **لا تحذف ملفات التوزيع** - السكريبت يحتاجها

---

**تاريخ الإنشاء:** 2025-11-09  
**المستودع:** https://github.com/Mh5en/asel-sys.git

