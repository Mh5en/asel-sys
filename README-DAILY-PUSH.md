# دليل الرفع اليومي التلقائي على GitHub

هذا الدليل يوضح كيفية إعداد نظام الرفع اليومي التلقائي على GitHub لمدة 30 يوم.

---

## 📋 المتطلبات

1. **Git** مثبت على النظام
2. **PowerShell** (متوفر افتراضيًا على Windows)
3. **GitHub Repository** جاهز
4. **صلاحيات المسؤول** (لإنشاء المهمة المجدولة)

---

## 🚀 خطوات الإعداد

### الخطوة 1: إعداد GitHub Repository

1. تأكد من وجود مستودع على GitHub
2. إذا لم يكن موجودًا، أنشئ مستودع جديد على: https://github.com/new
3. احفظ رابط المستودع (مثال: `https://github.com/username/asel-sys.git`)

### الخطوة 2: إعداد Git في المشروع

إذا لم يكن المشروع مهيأ لـ Git:

```powershell
git init
git remote add origin <YOUR_GITHUB_REPO_URL>
git add .
git commit -m "Initial commit"
git push -u origin master
```

### الخطوة 3: تشغيل سكريبت الإعداد

افتح PowerShell **كمسؤول** (Run as Administrator) وانتقل إلى مجلد المشروع:

```powershell
cd C:\Users\Mohamed\Desktop\asel-sys
.\setup-daily-push.ps1
```

سيطلب منك السكريبت:
- رابط GitHub Repository
- سيُنشئ مهمة مجدولة تلقائيًا

### الخطوة 4: التحقق من المهمة المجدولة

1. افتح **Task Scheduler** (جدولة المهام)
2. ابحث عن المهمة: `DailyGitHubPush-AselSys`
3. تحقق من أن المهمة مفعلة

---

## 🧪 اختبار السكريبت يدويًا

يمكنك تشغيل السكريبت يدويًا في أي وقت للاختبار:

```powershell
.\daily-git-push.ps1 -GitHubRepo "https://github.com/username/asel-sys.git"
```

أو بدون معامل إذا كان remote محدد بالفعل:

```powershell
.\daily-git-push.ps1
```

---

## 📝 كيف يعمل النظام؟

1. **كل يوم في الساعة 9:00 صباحًا**، يتم تشغيل السكريبت تلقائيًا
2. السكريبت يقوم بـ:
   - تحديث ملف `daily-commits-log.md` بتاريخ اليوم
   - إضافة التغييرات إلى Git
   - عمل commit برسالة تحتوي على التاريخ
   - رفع التغييرات إلى GitHub

---

## ⚙️ تخصيص الإعدادات

### تغيير وقت التشغيل

1. افتح **Task Scheduler**
2. ابحث عن المهمة: `DailyGitHubPush-AselSys`
3. انقر بزر الماوس الأيمن → **Properties**
4. اذهب إلى تبويب **Triggers**
5. عدّل الوقت المطلوب

### تغيير مدة الرفع

افتح ملف `daily-git-push.ps1` وعدّل السطر:

```powershell
$dayNumber = (New-TimeSpan -Start (Get-Date).AddDays(-30) -End (Get-Date)).Days
```

غيّر `-30` إلى العدد المطلوب من الأيام.

---

## 🔍 مراقبة الرفع اليومي

### 1. ملف السجل

افتح ملف `daily-commits-log.md` لرؤية سجل الرفع اليومي.

### 2. GitHub

اذهب إلى مستودعك على GitHub وافتح تبويب **Commits** لرؤية جميع الرفعات.

### 3. Task Scheduler

افتح **Task Scheduler** → **Task Scheduler Library** → `DailyGitHubPush-AselSys` → **History** لرؤية سجل التشغيل.

---

## 🛠️ استكشاف الأخطاء

### المشكلة: المهمة لا تعمل

**الحل:**
1. تحقق من أن المهمة مفعلة في Task Scheduler
2. تحقق من أن PowerShell يمكنه الوصول إلى الإنترنت
3. شغّل السكريبت يدويًا لرؤية الأخطاء

### المشكلة: فشل الرفع إلى GitHub

**الحل:**
1. تحقق من اتصال الإنترنت
2. تحقق من صلاحيات GitHub (قد تحتاج Personal Access Token)
3. تحقق من رابط المستودع: `git remote -v`

### المشكلة: لا توجد تغييرات للرفع

**الحل:**
هذا طبيعي إذا تم الرفع بالفعل في نفس اليوم. السكريبت سيرفع في اليوم التالي.

---

## 🗑️ إيقاف الرفع اليومي

### إيقاف مؤقت

1. افتح **Task Scheduler**
2. ابحث عن المهمة: `DailyGitHubPush-AselSys`
3. انقر بزر الماوس الأيمن → **Disable**

### حذف المهمة

1. افتح **Task Scheduler**
2. ابحث عن المهمة: `DailyGitHubPush-AselSys`
3. انقر بزر الماوس الأيمن → **Delete**

أو من PowerShell:

```powershell
Unregister-ScheduledTask -TaskName "DailyGitHubPush-AselSys" -Confirm:$false
```

---

## 📊 الإحصائيات

بعد 30 يوم، يمكنك:
- رؤية 30 commit على GitHub
- رؤية سجل كامل في ملف `daily-commits-log.md`
- تتبع تقدمك من خلال GitHub Insights

---

## 💡 نصائح

1. **احتفظ بنسخة احتياطية** من ملفات السكريبت
2. **اختبر السكريبت** قبل الاعتماد عليه
3. **راقب الرفع** في الأيام الأولى للتأكد من عمله
4. **استخدم Personal Access Token** إذا كان المستودع خاص

---

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من ملف `daily-commits-log.md`
2. شغّل السكريبت يدويًا لرؤية الأخطاء
3. تحقق من Task Scheduler History

---

**تم إنشاء هذا النظام بواسطة:** نظام أسيل  
**التاريخ:** $(Get-Date -Format "yyyy-MM-dd")

